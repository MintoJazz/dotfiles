return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    window = {
      auto_open_on_setup = true,
    },
    filesystem = {
      follow_current_file = {
        enabled = true,
      },
      hijack_netrw_behavior = "open_current",
    },
  },
}
